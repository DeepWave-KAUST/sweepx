# Examples

A gallery or list of worked examples. Link each to a runnable notebook or
script.

If the package ships Jupyter notebooks under `docs/`, the sweepx umbrella
renders them (mkdocs-jupyter) — list them here and link to them, e.g.
`[First example](notebooks/01_example.ipynb)`.
