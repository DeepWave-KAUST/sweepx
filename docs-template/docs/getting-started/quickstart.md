# Quickstart

The smallest end-to-end example — a handful of lines that produce a visible
result, so a new user sees the package work in under a minute.

```python
import IMPORT_NAME

# ... the minimal happy path ...
```

Explain the result in one or two sentences, then point onward to the
[User guide](../user-guide/index.md) and [Examples](../examples/index.md).
