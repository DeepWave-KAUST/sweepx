# Installation

```bash
pip install PACKAGE_NAME          # or: pip install sweepx  (bundles the ecosystem)
```

State any prerequisites here — Python version, GPU/CUDA needs, optional extras
(e.g. `pip install "PACKAGE_NAME[extra]"`), and how this package depends on the
rest of the `sweep` stack.

Verify the install:

```python
import IMPORT_NAME
print(IMPORT_NAME.__version__)
```
