# User guide

Overview of the guide. Explain the core concepts and the main workflow, then
split the details into their own pages under `user-guide/` and list them in the
`nav:` of this package's `mkdocs.yml`.

Suggested pages (rename/drop to fit the package):

- **Concepts** — the mental model and key objects.
- **Configuration** — inputs, schemas, options.
- **Workflows** — the end-to-end recipes users actually run.
