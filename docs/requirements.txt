# Docs toolchain for the sweepx umbrella build (mkdocs + multirepo + jupyter).
# The geophysics packages themselves (torch, sweep-solver, sweep-agent,
# sweep-tasks) are installed in the workflow — mkdocstrings imports them to
# render each package's API.
mkdocs>=1.6
mkdocs-material>=9.5
mkdocs-multirepo-plugin>=0.8
mkdocs-jupyter>=0.24
mkdocstrings[python]>=0.26
mkdocs-git-revision-date-localized-plugin>=1.2
black>=24                     # mkdocstrings-python wraps signatures at line_length
nbconvert>=7,<7.17            # >=7.17 dropped a filter mkdocs-jupyter still imports
